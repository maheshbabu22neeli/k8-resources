apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: roboshop

spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80  # not mandatory, nginx will automatically open port 80